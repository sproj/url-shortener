name: Check & Test common

on:
  pull_request:
    branches:
      - "main"
    paths:
      - "crates/common/**"

env:
  CARGO_TERM_COLOR: always
  ENV_TEST: 1
  TESTCONTAINERS_RYUK_DISABLED: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: Swatinem/rust-cache@v2.9.1
        with:
          shared-key: cargo-common-lint
      - name: check
        run: cargo check -p common
      - name: clippy
        run: cargo clippy -p common -- -D warnings
      - name: fmt
        run: cargo fmt --check

  unit-test:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: Swatinem/rust-cache@v2.9.1
        with:
          shared-key: cargo-common-unit-test
      - name: unit test
        run: cargo test -p common --lib

  integration-test:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: Swatinem/rust-cache@v2.9.1
        with:
          shared-key: cargo-common-integration-test
      - name: integration test
        run: cargo test -p common --tests
